const Engineer = require("../engineer");
const initInquire = require("../inquirer");

describe("Engineer", () => {
  describe("inheritance", () => {
    it("Should create a new engineer class, which inherits properties from the employee class", () => {});
  });
});
